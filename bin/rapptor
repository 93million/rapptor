#! /usr/bin/env sh

set -e

SCRIPT=$(realpath "$0")
SCRIPT_DIR=$(dirname "$SCRIPT")
PROJECT_DIR="$SCRIPT_DIR/.."

docker compose -f "$PROJECT_DIR/docker-compose.yml" run --rm plexmissioncontrolpanelservice npm --no-update-notifier --silent run cli -- $@
